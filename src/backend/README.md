# Backend (Option A)

Fastify + TypeScript API.

## Endpoints
- `POST /api/sessions`
- `GET /api/sessions/:sessionId/items/next`
- `POST /api/sessions/:sessionId/answers`
- `GET /api/sessions/:sessionId/score`

## Run
```bash
npm run dev:api
```
